{
  "name": "sportschat-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "install-all": "npm install && cd sportschat-frontend && npm install && cd ../sportschat-backend && npm install",
    "build": "cd sportschat-frontend && npm run build",
    "start": "node sportschat-backend/server.js"
  }
}